export interface MenuCategoryDto{
    categoryName: string;
    categoryIcon: string;
    categorySlug: string;
}

export interface MenuStoreDto{
    storeName:string;
    storeIcon:string;
    storeSlug:string;
}